Without Docker
1. Download the 

2. Unzip it, let's say in Applications/

3. Start the EzyFox Server:

    # On Windows, execute:
    console.bat
    # On other operating systems, execute:
    ./console.sh